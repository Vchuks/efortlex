
import DetailBox from "../molecules/DetailBox"


const DetailPage = () => {
  
    // const it = Items.map((each)=> <DetailBox key={each.id} item={each}/>)
  return (
    <div>
      <DetailBox />
    </div>
  )
}

export default DetailPage
